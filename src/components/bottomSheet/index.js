import {View, Text} from 'react-native';
import React from 'react';
import CustomModalWrapper from '../modal';

export default function BottoSheet() {
  return <CustomModalWrapper visbiility={true}></CustomModalWrapper>;
}
